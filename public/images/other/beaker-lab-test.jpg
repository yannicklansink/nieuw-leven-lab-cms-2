// Dit is een placeholder bestand voor een afbeelding.
// Omdat we geen echte afbeelding kunnen maken via tekst, moet je handmatig een afbeelding uploaden.
// Plaats een afbeelding van een laboratorium beaker of bloedtest op dit pad. 