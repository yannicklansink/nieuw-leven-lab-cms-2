---
description: 
globs: 
alwaysApply: false
---
---
description: 
globs: 
alwaysApply: false
---
name: "Nieuw Leven Lab Page Design"
description: "Cursor AI instructions for designing consistent Nieuw Leven Lab web pages"

project:
  name: Nieuw Leven Lab
  framework: Next.js
  language: TypeScript
  css_framework: Tailwind CSS
  ui_library: React + shadcn/ui

branding:
  colors:
    extra_light_green: "#F1F5F3"
    normal_green: "#477960"
    dark_green: "#09341F"
    black_headings_and_buttons: "#1A1A1A"
    white_bg: "#FFFFFF"
    beige_bg: "#EEE9E4"
    paragraaf: "#444444"
  
  - TypeScript Usage
    - Use TypeScript for all code; prefer interfaces over types.
    - Avoid enums; use maps instead.
    - Use functional components with TypeScript interfaces.

  typography:
    font_family: Geist by Vercel
    headings:
      - "Geist Bold"
      - sizes: "text-2xl md:text-4xl"
    subheadings:
      - "Geist SemiBold"
      - sizes: "text-xl md:text-2xl"
    body_text:
      - "Geist Regular"
      - sizes: "text-base md:text-lg"
    buttons:
      - "Geist Medium"

web_styling:
  layout:
    - Implement responsive design with Tailwind CSS; use a mobile-first approach.
    - padding: "p-4 md:p-6"
    - margin: "my-4 md:my-6"
    - rounded_corners: "rounded-lg md:rounded-xl"
    - shadow: "shadow-md"

  interactive_elements:
    buttons:
      primary:
        - bg_color: "bg-light_green"
        - text_color: "text-white"
        - hover_effect: "hover:bg-normal_green transition"
      secondary:
        - style: "border border-normal_green text-normal_green hover:bg-extra_light_green"
        - transition: "transition"

Components origanization

/app
├── /api
│   └── /products
│       └── route.ts // Leest data/products.json en retourneert gesimplificeerde productdata (slug, title, imageUrl, omschrijving_kort)
├── /product
│   └── /bloedtesten
│       └── /[slug]
│           └── page.tsx // Detailpagina voor een specifieke bloedtest
├── layout.tsx
├── page.tsx
└── globals.css
/data
├── products.json

/components
├── /ui
│   ├── # don't use this directory
├── /elements
│   └── Button.tsx
└── /sections
    └── VideoSection.tsx

media_guidelines:
  images:
    - rounded: "rounded-lg"
    - shadow: "shadow-md"
    - object_fit: "object-cover"

  icons:
    - style: "stroke-current"
    - size: "w-6 h-6 md:w-8 md:h-8"

image_optimization_and_storage:
  tooling:
    - "Gebruik altijd de Next.js `<Image>` component (`next/image`) voor prestatie-optimalisatie (WebP, lazy loading, CLS preventie)."
    - "Stel `width` en `height` in, of gebruik `layout='fill'` met een gepositioneerde parent container."
    - "Gebruik de `priority` prop voor afbeeldingen die direct zichtbaar zijn (above the fold / LCP element)."
  storage:
    - "Voor statische assets (logo, iconen): plaats in `/public` map."
    - "Voor dynamische/grote hoeveelheden afbeeldingen (producten): gebruik externe opslag (CDN, Cloud Storage, Headless CMS). Configureer `remotePatterns` in `next.config.js`."
  seo_and_accessibility:
    - "Gebruik beschrijvende, keyword-rijke bestandsnamen (bv. `vitamine-d-test.webp`)."
    - "Vul *altijd* de `alt` prop in met een duidelijke en relevante beschrijving."

seo:
  meta_description_length: "150-160 characters"
  og_image: "use consistent branding, minimalistic visuals"

accessibility:
  - aria_labels: true
  - focus_styles: "ring-2 ring-orange"

animations:
  - subtle_transitions: "transition duration-300 ease-in-out"

instruction_for_llm:
  - "Design consistent Nieuw Leven Lab pages following the guidelines above."
  - "Maintain mobile-first design approach, readability, and ease of navigation."
  - "Use provided colors, typography, and component styles strictly."